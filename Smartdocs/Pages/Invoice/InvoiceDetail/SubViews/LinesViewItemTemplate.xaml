<?xml version="1.0" encoding="UTF-8"?>
<ContentView 
	xmlns="http://xamarin.com/schemas/2014/forms" 
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	x:Class="Smartdocs.LinesViewItemTemplate"
	BackgroundColor="White"
	>
	<Grid BackgroundColor="White" RowSpacing="15" Padding="0, 15">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>
		<StackLayout Grid.Row="0" Grid.Column="0">
			<Label HorizontalOptions="Center" TextColor="Gray" Text="{Binding Line}" ></Label>
		</StackLayout>
		<StackLayout Grid.Row="0" Grid.Column="1">
			<Label HorizontalOptions="Center" TextColor="Gray" Text="{Binding Material}" ></Label>
		</StackLayout>
		<StackLayout Grid.Row="0" Grid.Column="2">
			<Label HorizontalOptions="Center" TextColor="Gray" Text="{Binding Amount}" ></Label>
		</StackLayout>
		<StackLayout Grid.Row="0" Grid.Column="3" Orientation="Horizontal">
			<Label HorizontalOptions="Center" TextColor="Gray" Text="{Binding Quantity}" ></Label>
			<Image Source="edit.png" WidthRequest="10" HeightRequest="10"></Image>
		</StackLayout>
	</Grid>
</ContentView>
